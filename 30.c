#include<stdio.h>
#define MAX_SIZE1000
void main()
{
int arr[MAX_SIZE];
int a,n;
printf("enter size of  array:");
scanf("%d",&n);
printf("enter %d elements in the array:" ,n);
for(a=0;a<n;a++)
{
scanf("%d",&arr[a])
}
printf("\n elements in array are:");
for(a=0;a<n;a++)
{
printf("%d",array[a]);
}
getch();
}
